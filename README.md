Always remember, that when you create a new repository, that you find the 'add new' file hyperlink (it's not a button, but a worded hyperlink) and then create your Index.html. 

This should now allow you to create a main branch, to commit files to the repo from the gitbash terminal in VSCode.


I am testing git push to edit this file locally and send the changes to the online repo.